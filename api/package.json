{
  "name": "apiforge-functions",
  "version": "1.0.0",
  "description": "Azure Functions backend for ApiForge request execution",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"Tests executed via root workspace\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "azure-functions-core-tools": "^4.0.5611",
    "rimraf": "^5.0.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
